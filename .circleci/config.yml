version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9
    steps:
      - checkout
      - run: npm install
      - run: npm test
      - run: cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js
